table:
  name: course_term_delivery_modes
  schema: public
object_relationships:
  - name: course
    using:
      foreign_key_constraint_on: course_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - course_id
        - term_id
        - delivery_mode
        - created_at
        - updated_at
      filter: {}
  - role: user
    permission:
      columns:
        - course_id
        - term_id
        - delivery_mode
        - created_at
        - updated_at
      filter: {}