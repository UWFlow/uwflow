- args:
    sql: CREATE TABLE "public"."prof_review"("id" serial NOT NULL, "course_id" integer
      , "prof_id" integer , "user_id" integer , "text" text , "clear" boolean , "engaging"
      boolean , PRIMARY KEY ("id") , FOREIGN KEY ("course_id") REFERENCES "public"."course"("id")
      ON UPDATE cascade ON DELETE set null, FOREIGN KEY ("prof_id") REFERENCES "public"."prof"("id")
      ON UPDATE cascade ON DELETE set null, FOREIGN KEY ("user_id") REFERENCES "public"."user"("id")
      ON UPDATE cascade ON DELETE set null);
  type: run_sql
- args:
    name: prof_review
    schema: public
  type: add_existing_table_or_view
