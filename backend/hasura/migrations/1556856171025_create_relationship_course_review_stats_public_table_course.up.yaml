- args:
    name: course_review_stats
    table:
      name: course
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: course_id
        remote_table:
          name: course_review_stats
          schema: aggregate
  type: create_object_relationship
